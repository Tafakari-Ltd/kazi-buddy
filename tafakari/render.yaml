services:
  - type: web
    name: django-api
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn tafakari.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: django-db
          property: connectionString

databases:
  - name: django-db
    plan: free
